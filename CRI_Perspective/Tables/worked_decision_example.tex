% Machine-generated by scripts/make_worked_example.py.
% Frozen run hash: 71d6a56c10a1c0ed.

\paragraph{Design and locked constants.}
The worked example is generated from the same frozen benchmark output directory as the operating-characteristics table. For run \texttt{71d6a56c10a1c0ed}, the representative supported injected-residual draw is replicate \(269\) with base seed \(102\), and the representative clean-null draw is replicate \(551\) with base seed \(101\).
The representative indices are read from \texttt{outputs/71d6a56c10a1c0ed/summary/representative\_index.json}. Headline decision objects are read from the frozen raw per-replicate CSV rows, while the bin means and participant slopes are recomputed deterministically from the same representative replicate and scenario configuration.
Both examples use \(P=24\) participants and the assigned-delay grid \(\tau_L\in\{0, 5, 10, 15, 20\}\,\mathrm{ms}\), with \(\sigma_\tau=0.00707\,\mathrm{s}\) on the second scale used by the slope estimator.
In the supported draw, the label-blind residual scale is \(\sigma^{\mathrm{blind}}_{\mathrm{resid}}=1.11\,\mu\mathrm{V}\), the retained-trial yield is \(\bar n_{\mathrm{ret}}=94.5\), and the registered resolution floor is
\[
\beta_{\min}=\kappa\,\frac{\sigma^{\mathrm{blind}}_{\mathrm{resid}}}{\sigma_\tau\sqrt{\bar n_{\mathrm{ret}}}}=32.3\,\mu\mathrm{V\,s^{-1}} .
\]
This floor is a resolvability threshold for the participant-level slope, not a biological importance threshold and not a population \(p\)-value.

\paragraph{Supported injected-residual draw.}
The injected-residual generator adds a negative endpoint-level slope before the comparator is fitted. The forward-only comparator is label-blind, the cross-fitted residuals are frozen, and the same decision rule used in the full benchmark is then applied.
The retained residual bin means in \Cref{tab:si-worked-bins} show the delay-ordered residual pattern for this representative draw. The participant slopes in \Cref{tab:si-worked-slopes} are the independent units entering the equal-participant estimand. Their mean is \(\widehat\beta_\tau=-60.0\,\mu\mathrm{V\,s^{-1}}\), with participant-level standard error \(4.7\,\mu\mathrm{V\,s^{-1}}\).

\begin{table}[t]
\centering
\caption{Retained residual-endpoint means by assigned-delay bin for the representative supported injected-residual draw. Means and standard errors are on the frozen residual endpoint scale in \(\mu\mathrm{V}\).}
\label{tab:si-worked-bins}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccccc}
\toprule
Assigned delay \(\tau_L\) (ms) & 0 & 5 & 10 & 15 & 20 \\
Centred \(\Delta\tau\) (ms) & -10 & -5 & 0 & 5 & 10 \\
\midrule
Retained trials & 448 & 458 & 447 & 452 & 463 \\
Mean \(A^{\mathrm{resid}}_{\mathrm{pre}}\) & 0.61 & 0.30 & -0.02 & -0.25 & -0.62 \\
SE & 0.05 & 0.05 & 0.05 & 0.05 & 0.05 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\centering
\caption{Participant slopes \(\widehat\beta_{\tau,p}\) for the representative supported injected-residual draw. Units are \(\mu\mathrm{V\,s^{-1}}\). The equal-participant mean is \(\widehat\beta_\tau=-60.0\).}
\label{tab:si-worked-slopes}
\footnotesize
\begin{tabular}{rrrrrrrr}
\toprule
-47.4 & -92.0 & -26.5 & -36.6 & -66.0 & -66.0 & -88.0 & -46.7 \\
-65.1 & -73.3 & -91.2 & -63.3 & -93.8 & -59.2 & -49.9 & -49.8 \\
-33.5 & -16.8 & -26.0 & -98.1 & -75.6 & -69.3 & -52.7 & -53.2 \\
\bottomrule
\end{tabular}
\end{table}

The assignment-calibrated randomisation test asks whether the observed negative slope is unusually negative under the assignment mechanism, with the frozen residual array held fixed. The participant-bootstrap bound asks whether the population participant-level slope is resolved beyond the registered floor. In this representative injected-residual draw, the randomisation criterion passes, the bootstrap-\(t\) upper bound lies below \(-\beta_{\min}\), the analysable participant count meets \(N_{\min}\), the support-blocking audits do not fire, the collider diagnostic does not fire, and the selection gate passes. The executable classifier therefore returns \texttt{supported}.

\paragraph{Clean-null draw and contrast.}
The clean-null draw uses the same endpoint construction, comparator, residual-freezing rule, inference, audits, and classifier, but its generator contains no assigned-delay residual. This example is not expected to give an exactly zero slope in finite Monte Carlo data. It is expected to fail the directional support rule while retaining a valid analysis sample. The final side-by-side trace in \Cref{tab:si-worked-decision} shows that the same locked pipeline supports the injected-residual draw and returns \texttt{forward\_only\_adequate} for the clean-null draw.

\begin{table}[t]
\centering
\caption{Every decision object available in the frozen per-replicate benchmark rows for the representative supported injected-residual draw and the representative clean-null draw. Slope, bound and floor units are \(\mu\mathrm{V\,s^{-1}}\).}
\label{tab:si-worked-decision}
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabularx}{\textwidth}{@{}p{3.9cm}p{3.0cm}p{3.0cm}p{3.0cm}@{}}
\toprule
Decision object & Criterion & Supported draw & Clean-null draw \\
\midrule
Scenario & recorded generator & injected residual & clean null \\
Replicate & representative index & \(269\) & \(551\) \\
Decision & executable classifier & \texttt{supported} & \texttt{forward\_only\_adequate} \\
Estimate \(\widehat\beta_\tau\) & reported & \(-60.0\) & \(0.1\) \\
Bootstrap-\(t\) \(\mathrm{UCB}_{0.95}\) & \(<-\beta_{\min}\) & \(-52.1\) & \(4.6\) \\
Bootstrap-\(t\) \(\mathrm{LCB}_{0.95}\) & reported & \(-67.2\) & \(-5.4\) \\
Resolution floor \(\beta_{\min}\) & registered formula & \(32.3\) & \(28.3\) \\
Randomisation \(p\), negative direction & \(\le 0.05\) & \(0.001\) & \(0.503\) \\
Randomisation \(p\), positive direction & reported & \(1.000\) & \(0.525\) \\
Support indicator & final outcome class & \(1\) & \(0\) \\
Selection-limited indicator & final outcome class & \(0\) & \(0\) \\
Diagnostic-failure indicator & final outcome class & \(0\) & \(0\) \\
Opposite-direction indicator & final outcome class & \(0\) & \(0\) \\
Null indicator & final outcome class & \(0\) & \(1\) \\
\bottomrule
\end{tabularx}
\end{table}
